build:
  docker:
    web: Dockerfile
run:
  web: sh -c "uv run python manage.py migrate --noinput && uv run gunicorn PharmaSFO.wsgi:application --bind 0.0.0.0:$PORT --workers 3 --timeout 60 --access-logfile -"